diff --git a/src/consensus/params.h b/src/consensus/params.h
index 99096e0..7676b14 100644
--- a/src/consensus/params.h
+++ b/src/consensus/params.h
@@ -6,6 +6,7 @@
 #ifndef BITCOIN_CONSENSUS_PARAMS_H
 #define BITCOIN_CONSENSUS_PARAMS_H
 
+#include <consensus/amount.h>
 #include <script/verify_flags.h>
 #include <uint256.h>
 
@@ -88,6 +89,14 @@ struct BIP9Deployment {
 struct Params {
     uint256 hashGenesisBlock;
     int nSubsidyHalvingInterval;
+
+    // Monetary policy parameters. Bitcoin networks retain the defaults below;
+    // Radium test networks override them explicitly in chainparams.
+    CAmount nMaxSupply{0};
+    CAmount nInitialSubsidy{50 * COIN};
+    int nSubsidyStartHeight{0};
+    CAmount founderAllocation{0};
+    int founderAllocationHeight{-1};
     /**
      * Hashes of blocks that
      * - are known to be consensus valid, and
diff --git a/src/kernel/chainparams.cpp b/src/kernel/chainparams.cpp
index 9764ba9..7fb30f2 100644
--- a/src/kernel/chainparams.cpp
+++ b/src/kernel/chainparams.cpp
@@ -568,7 +568,12 @@ public:
         m_chain_type = ChainType::REGTEST;
         consensus.signet_blocks = false;
         consensus.signet_challenge.clear();
-        consensus.nSubsidyHalvingInterval = 150;
+        consensus.nSubsidyHalvingInterval = 200'000;
+        consensus.nMaxSupply = 1'000'000 * COIN;
+        consensus.nInitialSubsidy = 237'500'000; // 2.375 RADS
+        consensus.nSubsidyStartHeight = 1;
+        consensus.founderAllocation = 50'000 * COIN;
+        consensus.founderAllocationHeight = 1;
         consensus.BIP34Height = 1; // Always active unless overridden
         consensus.BIP34Hash = uint256();
         consensus.BIP65Height = 1;  // Always active unless overridden
diff --git a/src/test/validation_tests.cpp b/src/test/validation_tests.cpp
index 268529f..6e7e7a0 100644
--- a/src/test/validation_tests.cpp
+++ b/src/test/validation_tests.cpp
@@ -45,6 +45,19 @@ static void TestBlockSubsidyHalvings(int nSubsidyHalvingInterval)
     TestBlockSubsidyHalvings(consensusParams);
 }
 
+BOOST_AUTO_TEST_CASE(radium_monetary_params_test)
+{
+    const auto chain_params = CreateChainParams(*m_node.args, ChainType::REGTEST);
+    const auto& consensus = chain_params->GetConsensus();
+
+    BOOST_CHECK_EQUAL(consensus.nMaxSupply, CAmount{1'000'000 * COIN});
+    BOOST_CHECK_EQUAL(consensus.nInitialSubsidy, CAmount{237'500'000});
+    BOOST_CHECK_EQUAL(consensus.nSubsidyStartHeight, 1);
+    BOOST_CHECK_EQUAL(consensus.nSubsidyHalvingInterval, 200'000);
+    BOOST_CHECK_EQUAL(consensus.founderAllocation, CAmount{50'000 * COIN});
+    BOOST_CHECK_EQUAL(consensus.founderAllocationHeight, 1);
+}
+
 BOOST_AUTO_TEST_CASE(block_subsidy_test)
 {
     const auto chainParams = CreateChainParams(*m_node.args, ChainType::MAIN);
