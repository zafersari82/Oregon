[package]
name = "oregon-contract-state"
version = "0.1.0"
edition = "2024"
rust-version = "1.85.0"

[dependencies]
oregon-primitives = { path = "../oregon-primitives" }
thiserror = "2"

[dev-dependencies]
proptest = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Test-first RED shell only. Removed when this crate joins the root workspace.
[workspace]
